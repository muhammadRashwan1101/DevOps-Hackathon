apiVersion: v1
kind: Service
metadata:
  name: BackBenchers-service
  namespace: BackBenchers
spec:
  type: NodePort
  selector:
    app: BackBenchers
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP